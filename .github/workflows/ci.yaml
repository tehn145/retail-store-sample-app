name: DevSecOps CI - Multi Services

on:
  push:
    branches: [ main ]
    paths:
      - 'src/**'

env:
  AWS_REGION: ap-southeast-1
  ENVIRONMENT: dev
  IMAGE_TAG: ${{ github.sha }}

jobs:
  build-and-secure:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        service:
          - name: ui
            context: ./src/ui
            ecr_repo: devsecops-dev-ui
          - name: catalog
            context: ./src/catalog
            ecr_repo: devsecops-dev-catalog
          - name: carts
            context: ./src/cart
            ecr_repo: devsecops-dev-carts
          - name: checkout
            context: ./src/checkout
            ecr_repo: devsecops-dev-checkout
          - name: orders
            context: ./src/orders
            ecr_repo: devsecops-dev-orders

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build image
        run: |
          docker build -t ${{ steps.login-ecr.outputs.registry }}/${{ matrix.service.ecr_repo }}:${{ env.IMAGE_TAG }} ${{ matrix.service.context }}

      - name: Run Trivy vulnerability scanner on image
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: '${{ steps.login-ecr.outputs.registry }}/${{ matrix.service.ecr_repo }}:${{ env.IMAGE_TAG }}'
          format: 'table'
          exit-code: '0'
          severity: 'CRITICAL,HIGH'

      - name: Push image to Amazon ECR
        run: |
          docker push ${{ steps.login-ecr.outputs.registry }}/${{ matrix.service.ecr_repo }}:${{ env.IMAGE_TAG }}

  update-gitops:
    runs-on: ubuntu-latest
    needs: build-and-secure

    steps:
      - name: Checkout GitOps repo
        uses: actions/checkout@v4
        with:
          repository: tehn145/devsecops-gitops-config
          token: ${{ secrets.GITOPS_TOKEN }}
          path: gitops

      - name: Update Kustomize image tags
        run: |
          cd gitops/k8s/overlays/${ENVIRONMENT}

          sed -i "s|name: .*devsecops-dev-ui.*|name: 252457242399.dkr.ecr.ap-southeast-1.amazonaws.com/devsecops-dev-ui|g" kustomization.yaml
          sed -i "s|newTag: .*|newTag: ${IMAGE_TAG}|g" kustomization.yaml

      - name: Commit and push GitOps changes
        run: |
          cd gitops
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          git add k8s/overlays/${ENVIRONMENT}/kustomization.yaml
          git commit -m "Auto-update image tags to ${IMAGE_TAG} via CI" || echo "No changes to commit"
          git push
